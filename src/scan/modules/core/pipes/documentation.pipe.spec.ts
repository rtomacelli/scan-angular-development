import { DocumentationPipe } from './documentation.pipe';

describe('DocumentationPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentationPipe();
    expect(pipe).toBeTruthy();
  });
});
