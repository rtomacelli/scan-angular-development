import { ToIdentifierPipe } from './to-identifier.pipe';

describe('ToIdentifierPipe', () => {
  it('create an instance', () => {
    const pipe = new ToIdentifierPipe();
    expect(pipe).toBeTruthy();
  });
});
