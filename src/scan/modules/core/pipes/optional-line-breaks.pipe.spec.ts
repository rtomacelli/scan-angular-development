import { OptionalLineBreaksPipe } from './optional-line-breaks.pipe';

describe('OptionalLineBreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionalLineBreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
