import { SortableLabelPipe } from './sortable-label.pipe';

describe('SortableLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SortableLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
