import { ClusterHighlightDirective } from './cluster-highlight.directive';

describe('ClusterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ClusterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
