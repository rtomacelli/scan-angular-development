import { DiagramHighlightDirective } from './diagram-highlight.directive';

describe('DiagramHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DiagramHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
