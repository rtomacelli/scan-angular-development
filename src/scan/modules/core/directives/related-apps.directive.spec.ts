import { RelatedAppsDirective } from './related-apps.directive';

describe('RelatedAppsDirective', () => {
  it('should create an instance', () => {
    const directive = new RelatedAppsDirective();
    expect(directive).toBeTruthy();
  });
});
